import React from 'react';

import styles from './styles.module.css';

const Header = () => (
  <div className={styles.headerContainer}>
    <p className={styles.headerContent}>Шагомер на тестовое задание</p>
  </div>
);

export default Header;